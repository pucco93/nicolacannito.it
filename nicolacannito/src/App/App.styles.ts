import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    container: {
        textAlign: 'left',
        fontFamily: 'Roboto',
        userSelect: 'none'
    }
});

export default useStyles;